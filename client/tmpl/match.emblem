
.prompt Join a bandwagon?

ul.rider-list
  each rider in ridersBesidesMe
    rider-list rider=rider me=../me join="addMeAsRider"

/ button click="addRand" Add Rand
button.full-input.btn.leave-btn click="removeMeAsRider" Throw yourself from vehicle?

pre.debug
  p Debug Me
  p NicK: #{me.nick}
  p ridersCount: #{me.riders.length}
  p capacity: #{me.capacity}
  p riders:
  ul
    each r in me.riders
      li= r.nick
  p ridingWith: #{me.ridingWith.nick}
  p isRiding: #{me.isRiding}
